# 學測考試總複習：交接文件

> 最後更新：2026-07-18
> 本專案由 **Claude Code 與 Codex 協同開發**。後續接手者（Claude Code 或 Codex）請先讀**規則正本**（Claude 讀 `CLAUDE.md`／Codex 讀 `AGENTS.md`，內容相同），再讀本文件了解架構與進度。

## 1. 專案目的

獨立的「學測考試總複習」互動刷題工具，跟 `vocab-app`（高中平時學習與月考前練習）分開。定位是學生高三大考前總複習：刷歷屆選擇題、自動對答案、錯題與弱點紀錄。

題庫來源檔目前是大考中心近三年（113/114/115）官方 PDF；**未來會有其他來源**（課本、月考考卷、模擬考考卷等，Word／PDF／照片），製作規則相同。

**未來開發定位（2026-07-10 傑克定）**：程式與題庫按**年級**組織（7 年級～12 年級），加上兩個總複習入口——**國中會考歷屆考題總複習**、**高中（學測）歷屆考題總複習**。本專案即「高中（學測）總複習」這一塊。實體資料夾為 `/Users/jackchan/開發項目/學測歷屆考題`（2026-07-20 從 `~/高三學測歷屆考題` 移入統一開發夾並改中文名；GitHub repo 仍是 gsat-exam，網址不變）。

## 2. 專案位置與線上網址

```text
開發資料夾：/Users/jackchan/開發項目/學測歷屆考題
線上：https://jackchan1973.github.io/gsat-exam/   （GitHub repo: jackchan1973/gsat-exam，公開）
```

## 3. 唯一主程式與架構

**所有互動題庫都整合進單一主程式 `public/index.html`，不可另開獨立頁。**

```text
public/index.html            主程式（掛載各題庫 JS 與 app.js）
 ├─ src/exam-files.js        EXAM_FILES：來源檔索引（提供科目/年度清單）
 ├─ src/question-bank/*.js   各科各年題庫（QUESTION_BANKS.push(...) 各自註冊）
 ├─ src/app.js               SPA：科目→年度→分類→答題卡→師長檢查（進度存 localStorage）
 └─ src/style.css            樣式
index.html（根目錄）          自動導向 public/index.html（給 GitHub Pages 用）
```

SPA 流程：**選科目 → 選年度 → 選分類 → 答題卡練習 → 交卷自動判分 → 師長檢查（完成率／錯題／進度）**。

app.js 支援的題型呈現：純文字單選/多選、題組共用資料（文章 sticky／字庫／句庫／圖表 passage-image）、克漏字空格、圖片選項、整題圖片題＋(1)-(5) 選項、選填數字格。舊題庫若仍使用圖片題組（displayGroups），後續重做時須改回答題卡標準格式。手機 360px 已驗收。

**答題卡標準格式（2026-07-13 起）**：所有新做或重做的答題卡，均比照 115 國文重做後的方式，使用 `blocks/questions` 結構、依正式卷順序呈現、題組共用資料只存一份、純文字題文字化、必要圖表/公式/插圖才裁官方局部圖、手寫題列出但不作答不判分。若遇到科目特性導致無法照此執行，先列出問題與 review_flags 給傑克確認。

## 4. 題庫產出流程

**預設製作方式：「文字題＋必要裁圖＋題組共用資料」**（詳見 CLAUDE.md／AGENTS.md）
- 純文字題盡量文字化（題幹、選項直接結構化）。
- 圖表、公式、插圖、無法可靠重建的版面，才裁來源檔局部圖。
- 題組的文章／表格／圖片只在題組層存一份，各題只存自己的題號、題幹、選項。
- 題目與答案完全依來源檔，不改、不猜。
- 數學或公式密集題可使用必要局部裁圖，但仍須遵守答題卡標準格式；無法照此執行時先列問題與 review_flags。

```text
來源檔（官方 PDF 等）──(tools/batch_extract.py)──▶ 中繼檔 data/raw-extract/{年}學年度/{科}/*.json
   （抽純文字/表格/圖片座標，只忠實搬運，不做判斷，省 token）
           │
           ▼
中繼檔 ──(build 腳本)──▶ src/question-bank/{年}-{科}.js（QUESTION_BANKS.push），在 index.html 掛一行 <script>
           │
           ▼
自動出現在主程式，走答題卡流程；答案取自來源答案檔，逐題核對零誤差才算數。
```

- 英文用**跨年度單一腳本** `tools/build_english.py {年}`：各年度差異只寫在腳本內 `CONFIG`。
- 需裁的圖輸出到 `public/images/{年}學年度/{科}/`，檔名帶題號或題組ID。

## 5. 目前進度（2026-07-18）

| 科目 | 已接進主程式 | 待做 |
|------|--------------|------|
| 國文 | 113、114、115 | 國寫作文不做互動作答 |
| 英文 | 113、114、115 | 無 |
| 數學A | 114、115 | 113 |
| 數學B | 無 | 113、114、115 |
| 社會 | 113、114、115 | 無 |
| 自然 | 113、114、115 | 無 |

目前 `src/question-bank/` 共有 14 份正式題庫：113/114/115 國文、英文、社會、自然，以及 114/115 數學A。

全站手寫題規則：非選擇題、作文、需書寫過程的題目要列入題庫並標示，但不提供作答與判分。

全部 81 個官方 PDF 已轉中繼檔（`data/raw-extract/`）。所有已上線題庫答案均須與官方核對零誤差。

## 6. 製作順序

優先補齊缺口：113 數學A、113/114/115 數學B。國寫作文不做互動題庫。

## 7. 部署

- GitHub Pages 從 `main` 分支發佈；更新＝`git push` 後約 1–2 分鐘生效。
- **發佈方式（2026-07-13 起）**：開發驗收通過後**直接推送發佈上線**，只提供線上版給傑克檢查；不得再交付或標示其他離線／開發用版本。
- 技術現況：`.gitignore` 排除 `資料/大考中心官方PDF/`、`*.pdf`、`.venv*/`、`tmp/` 等，來源原檔不進 repo、不公開。

## 8. 鐵律與原則

**正本在 `CLAUDE.md`／`AGENTS.md`**，摘要：
1. 提供的題庫來源檔與答案一律不可修改（不改寫、不修正、不推測、不補完）。
2. 答案取自來源答案檔，逐題核對零誤差才算完成。
3. 有疑義標 review_flags 交人工複核，不猜測。
4. 手寫題（非選擇題、作文）：放進題庫、列出題目，但不作答不判分；開發時回報手寫題清單（第幾題、共幾題）。
5. 題庫預設＝文字題＋必要裁圖＋題組共用資料。
6. 不建模擬題／測試分類。

## 9. 相關文件

- 規則正本：[CLAUDE.md](CLAUDE.md)／[AGENTS.md](AGENTS.md)
- 解析細則（題組、表格、手機驗收）：[docs/圖文混合題庫解析規範.md](docs/圖文混合題庫解析規範.md)
- 開發流程：[docs/開發流程.md](docs/開發流程.md)
- 給老師的參考與提示詞：[docs/用AI開發刷題工具_提示詞與流程.md](docs/用AI開發刷題工具_提示詞與流程.md)
- 個別開發紀錄：docs/115數A_開發紀錄.md
- 專案總覽：[README.md](README.md)
